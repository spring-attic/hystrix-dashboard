spring:
  application:
    name: hystrixdashboard
  cloud:
    config:
      discovery:
        enabled: true
eureka:
  password: password
  client:
    serviceUrl:
      defaultZone: http://user:${eureka.password}@localhost:8761/eureka/